class Solution {
    public int missingNumber(int[] nums) {
        int n = nums.length;
        int real=(n*(n+1))/2;
        int sum=0;
        for(int c:nums){
            sum+=c;
        }
        return real-sum;
    }
}
